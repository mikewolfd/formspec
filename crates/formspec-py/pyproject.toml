[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "formspec-rust"
requires-python = ">=3.10"
version = "0.1.0"
description = "PyO3 bindings for Formspec — Rust-powered FEL, linting, evaluation"

[tool.maturin]
features = ["pyo3/extension-module"]
