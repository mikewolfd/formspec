---
// Hero section — the value proposition above the fold.
// Light, clean, confident. Typography does the heavy lifting.
---

<section class="hero" aria-label="Introduction">
  <div class="hero-inner">

    <div class="hero-badge" aria-label="Project status">
      <span class="badge-dot" aria-hidden="true"></span>
      <span>Open source · MIT License</span>
    </div>

    <p class="hero-descriptor">An open-source form specification for high-stakes environments</p>

    <h1 class="hero-headline">
      Build complex forms<br />
      in an afternoon.<br />
      <span class="hero-headline-dim">They work everywhere. Even offline.</span>
    </h1>

    <p class="hero-sub">
      Describe what you need in plain English. AI builds a working form — complete with
      business rules, smart defaults, and multi-page workflows. Every form works offline,
      on any device, with no vendor lock-in.
    </p>

    <div class="hero-actions">
      <a href="/studio/" class="btn-primary">
        Try Formspec Studio
        <svg width="14" height="14" viewBox="0 0 14 14" fill="none" aria-hidden="true">
          <path d="M2.5 7H11.5M7.5 3L11.5 7L7.5 11" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </a>
      <a
        href="https://github.com/formspec/formspec"
        class="btn-ghost"
        target="_blank"
        rel="noopener noreferrer"
      >
        <svg width="16" height="16" viewBox="0 0 16 16" fill="currentColor" aria-hidden="true">
          <path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0016 8c0-4.42-3.58-8-8-8z"/>
        </svg>
        GitHub
      </a>
      <a href="/docs/" class="btn-ghost">
        Read the spec
      </a>
    </div>

    <div class="hero-install">
      <span class="hero-install-label">Add MCP to Claude Desktop:</span>
      <code class="hero-install-cmd" id="mcp-cmd">npx formspec-mcp</code>
      <button class="hero-install-copy" onclick="navigator.clipboard.writeText('npx formspec-mcp').then(() => { this.textContent = 'Copied'; setTimeout(() => this.textContent = 'Copy', 1500) })" aria-label="Copy install command">Copy</button>
    </div>

    <p class="hero-cta-caption">Studio is a browser-based form builder with AI chat. The MCP server adds 28 form-building tools to Claude, Cursor, or any MCP client.</p>

    <div class="hero-proof" role="list" aria-label="Key properties">
      <span role="listitem">Works offline</span>
      <span class="proof-sep" aria-hidden="true">/</span>
      <span role="listitem">Automatic validation</span>
      <span class="proof-sep" aria-hidden="true">/</span>
      <span role="listitem">No vendor lock-in</span>
      <span class="proof-sep" aria-hidden="true">/</span>
      <span role="listitem">AI-assisted</span>
      <span class="proof-sep" aria-hidden="true">/</span>
      <span role="listitem">Open source</span>
    </div>

  </div>

  <!-- Interactive playground — editable definition JSON and live rendered form -->
  <div class="hero-code">
    <div class="code-window split-window">
      <div class="code-window-bar">
        <span class="code-window-dot" style="background: #ff5f57;"></span>
        <span class="code-window-dot" style="background: #febc2e;"></span>
        <span class="code-window-dot" style="background: #28c840;"></span>
        <span class="code-filename">definition.json</span>
        <span class="split-window-label">Preview</span>
      </div>
      <div class="split-window-body">

        <!-- Left: editable JSON -->
        <div class="split-pane split-pane--code">
          <textarea
            id="hero-editor"
            class="hero-editor"
            spellcheck="false"
            autocomplete="off"
            autocorrect="off"
            autocapitalize="off"
            aria-label="Edit form definition JSON — changes update the preview"
          ></textarea>
          <div class="hero-editor-status" id="hero-editor-status"></div>
        </div>

        <!-- Right: live rendered form -->
        <div class="split-pane split-pane--preview" id="hero-preview-pane">
          <div class="preview-loading" id="hero-loading" aria-hidden="true">
            <div class="preview-loading-spinner"></div>
            <span>Rendering...</span>
          </div>
          <formspec-render id="hero-form"></formspec-render>
        </div>

      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    max-width: 76rem;
    margin: 0 auto;
    padding: 5rem clamp(1.25rem, 4vw, 2.5rem) 4rem;
    display: grid;
    grid-template-columns: 5fr 7fr;
    gap: 3rem;
    align-items: center;
  }

  .hero-inner {
    /* left column */
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.8125rem;
    font-weight: 500;
    color: var(--color-ink-secondary);
    background: var(--color-surface-raised);
    border: 1px solid var(--color-border);
    border-radius: 100px;
    padding: 0.25rem 0.75rem;
    margin-bottom: 1.75rem;
  }

  .badge-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--color-green);
    flex-shrink: 0;
  }

  .hero-descriptor {
    font-size: 0.875rem;
    color: var(--color-ink-secondary);
    margin: 0 0 0.75rem;
    line-height: 1.5;
  }

  .hero-install {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 1rem;
    flex-wrap: wrap;
  }

  .hero-install-label {
    font-size: 0.8125rem;
    font-weight: 500;
    color: var(--color-ink-tertiary);
  }

  .hero-install-cmd {
    font-family: var(--font-mono);
    font-size: 0.8125rem;
    background: var(--color-surface-sunken);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    padding: 0.3rem 0.625rem;
    color: var(--color-ink);
    user-select: all;
  }

  .hero-install-copy {
    font-family: var(--font-mono);
    font-size: 0.6875rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    padding: 0.3rem 0.5rem;
    border-radius: var(--radius-sm);
    border: 1px solid var(--color-border);
    background: var(--color-surface);
    color: var(--color-ink-secondary);
    cursor: pointer;
    transition: border-color 0.15s, color 0.15s;
  }

  .hero-install-copy:hover {
    border-color: var(--color-accent);
    color: var(--color-accent);
  }

  .hero-cta-caption {
    font-size: 0.8125rem;
    color: var(--color-ink-tertiary);
    margin: 0 0 2rem;
    line-height: 1.5;
  }

  .hero-headline {
    font-size: clamp(2.25rem, 4.5vw, 3.25rem);
    font-weight: 700;
    letter-spacing: -0.035em;
    line-height: 1.1;
    color: var(--color-ink);
    margin: 0 0 1.5rem;
  }

  .hero-headline-dim {
    color: var(--color-ink-tertiary);
  }

  .hero-sub {
    font-size: 1.0625rem;
    color: var(--color-ink-secondary);
    line-height: 1.7;
    max-width: 38rem;
    margin: 0 0 2rem;
  }

  .hero-actions {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    flex-wrap: wrap;
    margin-bottom: 2rem;
  }

  .btn-primary {
    display: inline-flex;
    align-items: center;
    gap: 0.375rem;
    font-size: 0.9375rem;
    font-weight: 600;
    padding: 0.625rem 1.25rem;
    border-radius: var(--radius);
    background: var(--color-accent);
    color: white;
    text-decoration: none;
    transition: background-color 0.15s, transform 0.1s;
    box-shadow: 0 1px 2px rgb(61 82 213 / 0.3), 0 0 0 1px rgb(61 82 213 / 0.1);
  }

  .btn-primary:hover {
    background: #2c42c4;
    transform: translateY(-1px);
  }

  .btn-ghost {
    display: inline-flex;
    align-items: center;
    gap: 0.375rem;
    font-size: 0.9375rem;
    font-weight: 500;
    padding: 0.625rem 1.25rem;
    border-radius: var(--radius);
    border: 1px solid var(--color-border-strong);
    color: var(--color-ink-secondary);
    text-decoration: none;
    background: var(--color-surface);
    transition: border-color 0.15s, color 0.15s;
  }

  .btn-ghost:hover {
    border-color: var(--color-accent);
    color: var(--color-accent);
  }

  .hero-proof {
    display: flex;
    align-items: center;
    gap: 0.625rem;
    flex-wrap: wrap;
    font-size: 0.8125rem;
    color: var(--color-ink-tertiary);
  }

  .proof-sep {
    color: var(--color-border-strong);
  }

  .hero-code {
    min-width: 0;
  }

  .code-filename {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: rgb(255 255 255 / 0.4);
    margin-left: 0.5rem;
  }

  /* ── Split window layout ── */

  .split-window {
    overflow: hidden;
  }

  .split-window-label {
    margin-left: auto;
    font-family: var(--font-mono);
    font-size: 0.6875rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: rgb(255 255 255 / 0.25);
    padding: 0.2rem 0.5rem;
    border: 1px solid rgb(255 255 255 / 0.1);
    border-radius: 3px;
  }

  .split-window-body {
    display: grid;
    grid-template-columns: 55fr 45fr;
    min-height: 0;
  }

  .split-pane {
    min-width: 0;
    overflow: hidden;
  }

  /* ── Left: editor pane ── */

  .split-pane--code {
    border-right: 1px solid rgb(255 255 255 / 0.07);
    position: relative;
    display: flex;
    flex-direction: column;
  }

  .hero-editor {
    flex: 1;
    background: transparent;
    color: #c9d1d9;
    font-family: var(--font-mono);
    font-size: 0.75rem;
    line-height: 1.65;
    padding: 1rem 1.125rem 1rem 0.75rem;
    border: none;
    outline: none;
    resize: none;
    tab-size: 2;
    white-space: pre;
    overflow: auto;
  }

  .hero-editor::selection {
    background: rgb(88 110 230 / 0.35);
  }

  .hero-editor-status {
    position: absolute;
    bottom: 0.5rem;
    right: 0.75rem;
    font-family: var(--font-mono);
    font-size: 0.625rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    padding: 0.15rem 0.4rem;
    border-radius: 2px;
    opacity: 0;
    transition: opacity 0.2s;
    pointer-events: none;
  }

  .hero-editor-status.error {
    color: #ff6b6b;
    background: rgb(255 107 107 / 0.1);
    opacity: 1;
  }

  .hero-editor-status.ok {
    color: #50fa7b;
    background: rgb(80 250 123 / 0.1);
    opacity: 1;
  }

  /* ── Right: preview pane ── */

  .split-pane--preview {
    background: #f8f8f7;
    padding: 1rem 1.125rem;
    overflow-y: auto;
    position: relative;
    transition: opacity 0.15s;
  }

  .split-pane--preview.is-pending formspec-render {
    opacity: 0.3;
    filter: blur(1px);
    transition: opacity 0.1s, filter 0.1s;
  }

  .split-pane--preview formspec-render {
    font-size: 0.8125rem;
    transition: opacity 0.2s, filter 0.2s;
  }

  .split-pane--preview .formspec-submit {
    margin-top: 0.75rem;
    padding: 0.5rem 1.25rem;
    font-size: 0.8125rem;
    font-weight: 600;
    border: none;
    border-radius: 6px;
    background: var(--color-accent, #3d52d5);
    color: white;
    cursor: pointer;
    transition: background 0.15s;
  }

  .split-pane--preview .formspec-submit:hover {
    background: #2c42c4;
  }

  /* Loading overlay */
  .preview-loading {
    position: absolute;
    inset: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    z-index: 2;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.15s;
  }

  .split-pane--preview.is-pending .preview-loading {
    opacity: 1;
  }

  .preview-loading span {
    font-family: var(--font-mono);
    font-size: 0.6875rem;
    font-weight: 500;
    color: #6b7b8d;
    letter-spacing: 0.02em;
  }

  .preview-loading-spinner {
    width: 18px;
    height: 18px;
    border: 2px solid #ddd;
    border-top-color: var(--color-accent, #3d52d5);
    border-radius: 50%;
    animation: hero-spin 0.6s linear infinite;
  }

  @keyframes hero-spin {
    to { transform: rotate(360deg); }
  }

  @media (max-width: 900px) {
    .hero {
      grid-template-columns: 1fr;
      gap: 2.5rem;
      padding-top: 3rem;
    }

    .hero-code {
      max-width: 100%;
    }
  }

  @media (max-width: 600px) {
    .hero-headline {
      font-size: clamp(2rem, 8vw, 2.5rem);
    }

    .split-window-body {
      grid-template-columns: 1fr;
    }

    .split-pane--code {
      border-right: none;
      border-bottom: 1px solid rgb(255 255 255 / 0.07);
      min-height: 200px;
    }
  }
</style>

<script>
  import { FormspecRender } from 'formspec-webcomponent';
  import registry from '../../../registries/formspec-common.registry.json';

  if (!customElements.get('formspec-render')) {
    customElements.define('formspec-render', FormspecRender);
  }

  const initialDef = {
    "$formspec": "1.0",
    "url": "https://formspec.dev/demo/grant-application",
    "title": "Grant Application",
    "items": [
      {
        "type": "field",
        "key": "org_type",
        "dataType": "choice",
        "label": "Organization Type",
        "options": [
          { "value": "nonprofit", "label": "Nonprofit (501c3)" },
          { "value": "government", "label": "Government" },
          { "value": "tribal", "label": "Tribal Organization" }
        ]
      },
      {
        "type": "field", "key": "ein", "dataType": "string",
        "label": "EIN", "extensions": { "x-formspec-ein": true }
      },
      { "type": "field", "key": "amount", "dataType": "money", "label": "Request Amount" },
      { "type": "field", "key": "duration", "dataType": "number", "label": "Duration (months)" },
      { "type": "field", "key": "budget", "dataType": "money", "label": "Monthly Budget" }
    ],
    "binds": [
      { "path": "ein", "relevant": "org_type = 'nonprofit'", "required": "true" },
      { "path": "budget", "calculate": "amount / duration", "readonly": "true" }
    ],
    "formPresentation": { "defaultCurrency": "USD" }
  };

  const form = document.getElementById('hero-form') as FormspecRender | null;
  const editor = document.getElementById('hero-editor') as HTMLTextAreaElement | null;
  const status = document.getElementById('hero-editor-status');
  const previewPane = document.getElementById('hero-preview-pane');

  /** Map definition items → component tree nodes + a SubmitButton at the end. */
  function buildComponentDoc(def: any) {
    const children: any[] = [];
    for (const item of def.items || []) {
      if (item.type === 'field') {
        const dt = item.dataType;
        let component = 'TextInput';
        if (dt === 'choice') component = 'Select';
        else if (dt === 'number') component = 'NumberInput';
        else if (dt === 'money') component = 'MoneyInput';
        else if (dt === 'date') component = 'DatePicker';
        else if (dt === 'boolean') component = 'Toggle';
        children.push({ component, bind: item.key });
      }
    }
    children.push({ component: 'SubmitButton', label: 'Submit Application' });
    return {
      $formspecComponent: '1.0',
      version: '1.0.0',
      targetDefinition: { url: def.url },
      tree: { component: 'Stack', children },
    };
  }

  function applyDefinition(def: any) {
    if (!form) return;
    def.$formspec = def.$formspec || '1.0';
    def.url = def.url || 'https://formspec.dev/demo/grant-application';
    form.registryDocuments = registry;
    form.componentDocument = buildComponentDoc(def);
    form.definition = def;
  }

  function prefillValues() {
    // Engine is created synchronously by the definition setter,
    // but we need to wait for the render pass to complete
    // before setting values so the DOM reflects them.
    setTimeout(() => {
      const engine = form?.getEngine?.();
      if (engine) {
        engine.setValue('org_type', 'nonprofit');
        engine.setValue('ein', '47-0123456');
        engine.setValue('amount', { amount: 100000, currency: 'USD' });
        engine.setValue('duration', 12);
      }
    }, 0);
  }

  function showStatus(type: 'ok' | 'error', text: string) {
    if (!status) return;
    status.textContent = text;
    status.className = 'hero-editor-status ' + type;
    if (type === 'ok') {
      setTimeout(() => { status.className = 'hero-editor-status'; }, 1200);
    }
  }

  function setPending(on: boolean) {
    if (!previewPane) return;
    if (on) {
      previewPane.classList.add('is-pending');
    } else {
      previewPane.classList.remove('is-pending');
    }
  }

  if (editor && form) {
    // Populate textarea with pretty-printed JSON (strip envelope fields for cleaner display)
    const { $formspec, url, ...display } = initialDef;
    editor.value = JSON.stringify(display, null, 2);

    // Initial render
    applyDefinition(initialDef);
    prefillValues();

    // Live editing — show pending state immediately, debounce the actual render
    let timer: ReturnType<typeof setTimeout>;
    editor.addEventListener('input', () => {
      clearTimeout(timer);
      setPending(true);
      showStatus('ok', ''); // clear any previous status
      status!.className = 'hero-editor-status'; // hide status while typing

      timer = setTimeout(() => {
        try {
          const parsed = JSON.parse(editor.value);
          applyDefinition(parsed);
          prefillValues();
          setPending(false);
          showStatus('ok', 'Updated');
        } catch {
          setPending(false);
          showStatus('error', 'Invalid JSON');
        }
      }, 600);
    });

    // Tab inserts spaces
    editor.addEventListener('keydown', (e) => {
      if (e.key === 'Tab') {
        e.preventDefault();
        const start = editor.selectionStart;
        const end = editor.selectionEnd;
        editor.value = editor.value.substring(0, start) + '  ' + editor.value.substring(end);
        editor.selectionStart = editor.selectionEnd = start + 2;
        editor.dispatchEvent(new Event('input'));
      }
    });
  }
</script>
