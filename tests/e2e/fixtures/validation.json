{
  "$formspec": "1.0",
  "url": "http://example.org/fixtures/validation",
  "version": "1.0.0",
  "status": "active",
  "title": "Validation Test",
  "items": [
    {
      "type": "field", "dataType": "integer",
      "key": "startDate",
      "label": "Start Date (Number)"
    },
    {
      "type": "field", "dataType": "integer",
      "key": "endDate",
      "label": "End Date (Number)"
    }
  ],
  "binds": [
    {
      "target": "endDate",
      "constraint": "endDate >= startDate",
      "message": "End date must be >= start date"
    }
  ]
}
