{
  "title": "Spec Artifacts Configuration",
  "description": "Declares spec-schema pairings and behavior/conformance essentials for LLM artifact generation.",
  "specs": [
    {
      "spec": "specs/theme/theme-spec.md",
      "schema": "schemas/theme.schema.json",
      "bluf": "specs/theme/theme-spec.bluf.md",
      "llm": "specs/theme/theme-spec.llm.md",
      "semanticCapsule": "specs/theme/theme-spec.semantic.md",
      "behaviorEssentials": [
        "Resolve presentation using deterministic cascade precedence: defaults, then matching selectors in document order, then item-specific overrides.",
        "Apply token references in style and widget configuration at theme-application time; unresolved tokens should fall back to renderer defaults with diagnostics.",
        "Treat platform as an open string hint; unknown platform values should not block theme application.",
        "When preferred widgets are unavailable, apply declared fallback chains before reverting to renderer-native defaults."
      ],
      "conformanceEssentials": [
        "A conforming theme document must include $formspecTheme=1.0, version, and targetDefinition.",
        "Renderers must not let theme content alter core data semantics, validation semantics, or response payload shape.",
        "Implementations should validate targetDefinition compatibility ranges and warn on mismatch rather than hard-failing rendering."
      ]
    },
    {
      "spec": "specs/mapping/mapping-spec.md",
      "schema": "schemas/mapping.schema.json",
      "bluf": "specs/mapping/mapping-spec.bluf.md",
      "llm": "specs/mapping/mapping-spec.llm.md",
      "semanticCapsule": "specs/mapping/mapping-spec.semantic.md",
      "behaviorEssentials": [
        "Execute rules in deterministic order after defaults and direction resolution; higher priority executes first and same-priority rules preserve document order.",
        "Evaluate condition guards before transform application; skipped rules must not write target values.",
        "Support explicit reverse overrides and bidirectional flags to control reverse execution behavior.",
        "Treat adapters as serialization boundaries: mapping evaluates against structured data before format-specific output encoding."
      ],
      "conformanceEssentials": [
        "A conforming mapping document must include version, definitionRef, definitionVersion, targetSchema, and at least one rule.",
        "Processors must reject invalid mapping documents and unknown transform declarations rather than silently ignoring them.",
        "Reverse execution through non-reversible transforms must be rejected or explicitly disabled by rule configuration."
      ]
    },
    {
      "spec": "specs/core/spec.md",
      "schema": "schemas/response.schema.json",
      "bluf": "specs/core/response-spec.bluf.md",
      "llm": "specs/core/response-spec.llm.md",
      "semanticCapsule": "specs/core/response-spec.semantic.md",
      "behaviorEssentials": [
        "Response documents are bound to an exact definition identity tuple and must be interpreted against that pinned version.",
        "Response status transitions are business-process concerns, but completed responses must satisfy error-level validation expectations.",
        "Validation findings can be carried inline through validationResults without changing the canonical response data payload.",
        "Optional metadata fields (author, subject, id) should not alter structural validity of required response content."
      ],
      "conformanceEssentials": [
        "A conforming response must include definitionUrl, definitionVersion, status, data, and authored.",
        "Processors must reject responses that do not conform to the response schema shape.",
        "Pinned definition references must not be silently substituted with different versions during processing."
      ]
    },
    {
      "spec": "specs/core/spec.md",
      "schema": "schemas/validation-report.schema.json",
      "bluf": "specs/core/validation-report-spec.bluf.md",
      "llm": "specs/core/validation-report-spec.llm.md",
      "semanticCapsule": "specs/core/validation-report-spec.semantic.md",
      "behaviorEssentials": [
        "ValidationReport aggregates all findings at a point in time and should be treated as an immutable evaluation snapshot.",
        "The valid flag must be derived from error-level findings, not from warning or info counts.",
        "Counts must remain consistent with results so report summaries and downstream decisions stay deterministic."
      ],
      "conformanceEssentials": [
        "A conforming validation report must include valid, results, counts, and timestamp.",
        "ValidationReport results entries must conform to the shared validation-result.schema.json schema.",
        "Processors should reject reports where required severity counts are missing or invalid."
      ]
    },
    {
      "spec": "specs/core/spec.md",
      "schema": "schemas/definition.schema.json",
      "bluf": "specs/core/definition-spec.bluf.md",
      "llm": "specs/core/definition-spec.llm.md",
      "semanticCapsule": "specs/core/definition-spec.semantic.md",
      "behaviorEssentials": [
        "Definition documents declare structure, logic hooks, and extension points but do not execute behavior by themselves.",
        "Top-level lifecycle fields (status, version, versionAlgorithm) determine publication and compatibility semantics across versions.",
        "Definition-level defaults and options (for example formPresentation and nonRelevantBehavior) establish baseline processing and rendering behavior.",
        "Behavioral logic remains declarative through binds and shapes and is evaluated by conformant processors at runtime."
      ],
      "conformanceEssentials": [
        "A conforming definition must include $formspec, url, version, status, title, and items.",
        "Processors must reject definitions that violate structural constraints or unsupported $formspec versions.",
        "The (url, version) pair must be treated as immutable identity for definition pinning and response validation."
      ]
    },
    {
      "spec": "specs/registry/extension-registry.md",
      "schema": "schemas/registry.schema.json",
      "bluf": "specs/registry/extension-registry.bluf.md",
      "llm": "specs/registry/extension-registry.llm.md",
      "semanticCapsule": "specs/registry/extension-registry.semantic.md",
      "behaviorEssentials": [
        "Registry documents describe extension metadata and compatibility, but do not prescribe centralized hosting or governance.",
        "Entry lifecycle states should guide consumer behavior for adoption, warning surfaces, and retirement handling.",
        "Naming conventions and uniqueness constraints prevent collision and support cross-organization interoperability."
      ],
      "conformanceEssentials": [
        "A conforming registry document must include $formspecRegistry, publisher, published, and entries.",
        "Registry entry names must follow x-prefixed naming rules and category-specific requirements.",
        "Processors should reject malformed registries and conflicting duplicate name/version entry pairs."
      ]
    },
    {
      "spec": "specs/registry/changelog-spec.md",
      "schema": "schemas/changelog.schema.json",
      "bluf": "specs/registry/changelog-spec.bluf.md",
      "llm": "specs/registry/changelog-spec.llm.md",
      "semanticCapsule": "specs/registry/changelog-spec.semantic.md",
      "behaviorEssentials": [
        "Changelog entries represent atomic diffs between two definition versions and should be processed in document order.",
        "Impact classification should be derived deterministically from change semantics to drive semver governance.",
        "Migration hints are advisory mappings that assist generation of formal migration artifacts."
      ],
      "conformanceEssentials": [
        "A conforming changelog document must include definitionUrl, fromVersion, toVersion, semverImpact, and changes.",
        "Change objects must include type, target, path, and impact, with optional before/after details as applicable.",
        "semverImpact must reflect the maximum impact level represented in the change set."
      ]
    },
    {
      "spec": "specs/component/component-spec.md",
      "schema": "schemas/component.schema.json",
      "bluf": "specs/component/component-spec.bluf.md",
      "llm": "specs/component/component-spec.llm.md",
      "semanticCapsule": "specs/component/component-spec.semantic.md",
      "behaviorEssentials": [
        "Component documents define a single-root presentation tree where child order is rendered deterministically.",
        "Slot binding links input components to definition item keys while preserving core behavioral semantics from the definition.",
        "Tier 3 components take precedence over theme and inline hints for layout/widget selection, with fallback rendering for unbound items.",
        "Core processors must substitute progressive components with declared fallbacks while preserving structure and applicable base props."
      ],
      "conformanceEssentials": [
        "A conforming component document must include $formspecComponent, version, targetDefinition, and tree.",
        "Processors must reject unsupported component-document versions and unknown non-x top-level properties.",
        "Renderable trees must satisfy component-category constraints, bind compatibility, and declared structural limits."
      ]
    },
    {
      "spec": "specs/locale/locale-spec.md",
      "schema": "schemas/locale.schema.json",
      "bluf": "specs/locale/locale-spec.bluf.md",
      "llm": "specs/locale/locale-spec.llm.md",
      "behaviorEssentials": [
        "Locale Documents are display-only sidecars — they MUST NOT affect data collection, validation logic, or behavioral semantics.",
        "Resolve localized strings using a deterministic cascade: regional locale → base language locale → inline Definition defaults.",
        "String keys use dot-delimited paths (itemKey.property) to target specific localizable properties; unmatched keys are silently ignored.",
        "FEL interpolation via {{expression}} syntax is evaluated at render time against current form state; broken expressions fall back to the raw template string with a diagnostic."
      ],
      "conformanceEssentials": [
        "A conforming locale document must include $formspecLocale=1.0, version, locale, targetDefinition, and strings.",
        "Processors MUST perform case-insensitive BCP 47 tag comparison and SHOULD normalize to lowercase language with title-case region.",
        "Processors MUST detect circular fallback chains and terminate the cascade with a warning rather than looping."
      ]
    },
    {
      "spec": "specs/core/references-spec.md",
      "schema": "schemas/references.schema.json",
      "llm": "specs/core/references-spec.llm.md",
      "behaviorEssentials": [
        "References Documents are standalone sidecars that bind external resources to Definition items by path — they MUST NOT alter data capture, validation, or the processing model.",
        "Each Bound Reference targets a specific item path (or '#' for form-level); references do not inherit from parent groups to children.",
        "The type and rel properties are open strings for forward compatibility — processors SHOULD warn on unrecognized values but MUST NOT reject the document.",
        "Multiple References Documents may target the same Definition; their references are merged additively with document-internal order preserved."
      ],
      "conformanceEssentials": [
        "A conforming references document must include $formspecReferences=1.0, version, targetDefinition, and references.",
        "Extended processors must verify targetDefinition.url matches the loaded Definition and resolve all $ref pointers at load time.",
        "Extended processors must validate referenceDefs id-key matching and should warn on target paths that don't correspond to definition items."
      ]
    },
    {
      "spec": "wos-spec/specs/kernel/spec.md",
      "schema": "wos-spec/schemas/kernel/wos-kernel.schema.json",
      "llm": "wos-spec/specs/kernel/spec.llm.md",
      "behaviorEssentials": [
        "Kernel documents define the immutable topology (states and transitions) and case state of a workflow orchestration substrate.",
        "The case file structure SHOULD mirror the data-capture definitions used for input in the workflow.",
        "Transition logic must be deterministic; choice states MUST have exhaustive, mutually exclusive conditions.",
        "Impact level classification established in the kernel determines baseline governance constraints for the entire workflow lifecycle."
      ],
      "conformanceEssentials": [
        "A conforming kernel document must include $wosKernel=1.0, id, version, status, title, and topology.",
        "Processors must reject kernel documents that do not conform to the kernel schema shape.",
        "The (id, version) pair must be treated as immutable identity for pinning and execution."
      ]
    },
    {
      "spec": "wos-spec/specs/governance/workflow-governance.md",
      "schema": "wos-spec/schemas/governance/wos-workflow-governance.schema.json",
      "llm": "wos-spec/specs/governance/workflow-governance.llm.md",
      "behaviorEssentials": [
        "Workflow Governance sidecars attach to kernel workflows via semantic tags (lifecycleHook) and data contracts (contractHook).",
        "Due process requirements (notice, explanation, appeal) are normative for rights-impacting or safety-impacting impact levels.",
        "Review protocols like independentFirst must be enforced by the processor and UI to ensure cognitive engagement.",
        "Data validation pipelines with assertion gates validate untrusted external data before it enters the workflow case state."
      ],
      "conformanceEssentials": [
        "A conforming governance document must include $wosWorkflowGovernance=1.0, version, and targetWorkflow.",
        "Processors must verify that the targetWorkflow URL matches the loaded kernel document.",
        "Governance rules targeting non-existent tags or roles should produce warnings rather than hard-failing."
      ]
    },
    {
      "spec": "wos-spec/specs/ai/ai-integration.md",
      "schema": "wos-spec/schemas/ai/wos-ai-integration.schema.json",
      "llm": "wos-spec/specs/ai/ai-integration.llm.md",
      "behaviorEssentials": [
        "AI Integration sidecars register agents as actors and impose deontic constraints (Permission, Prohibition, Obligation, Right) on their behavior.",
        "Effective autonomy levels for agent actions are capped by the kernel impactLevel; rights-impacting workflows default to assistive.",
        "The Formspec-as-validator pattern cages agent output as untrusted input validated against the same contract used by humans.",
        "Narrative tier provenance generated by models MUST be explicitly labeled as non-authoritative."
      ],
      "conformanceEssentials": [
        "A conforming AI integration document must include $wosAIIntegration=1.0, version, and targetWorkflow.",
        "Processor guardrail enforcement MUST execute in order: Permission -> Prohibition -> Obligation -> Confidence Floor.",
        "Fallback chains MUST terminate in a human-action result or failure to ensure graceful degradation."
      ]
    },
    {
      "spec": "wos-spec/specs/advanced/advanced-governance.md",
      "schema": "wos-spec/schemas/advanced/wos-advanced.schema.json",
      "llm": "wos-spec/specs/advanced/advanced-governance.llm.md",
      "behaviorEssentials": [
        "Advanced Governance introduces DCR-style Constraint Zones for declarative, adaptive case management within compound states.",
        "Equity guardrails monitor for statistical outcomes disparity across demographic or categorical case groupings.",
        "Formal verification via SMT solvers targets a decidable fragment of the FEL expression language for provenance-safe proofs.",
        "Operational resilience patterns like shadow mode and circuit breakers protect production stability from agent behavioral drift."
      ],
      "conformanceEssentials": [
        "A conforming advanced governance document must include $wosAdvancedGovernance=1.0, version, and targetWorkflow.",
        "DCR zone markings (included, pending, executed) must be tracked immutably within the session context.",
        "SMT verification inconclusive results should trigger additional runtime monitoring warnings rather than blocking execution."
      ]
    },
    {
      "spec": "wos-spec/specs/kernel/correspondence-metadata.md",
      "schema": "wos-spec/schemas/kernel/wos-correspondence-metadata.schema.json",
      "llm": "wos-spec/specs/kernel/correspondence-metadata.llm.md",
      "behaviorEssentials": [
        "Declare structured metadata for correspondence stored in case state without adding new event types.",
        "Use entry templates to define channel, direction, and actorType requirements for categories of communication.",
        "Populate base metadata (templateRef, contentRef, summary, timestamp) for every recorded correspondence entry.",
        "Store pointers to external content (contentRef) rather than the content itself within the case record."
      ],
      "conformanceEssentials": [
        "A conforming correspondence metadata document must include $wosCorrespondenceMetadata=1.0 and targetWorkflow.",
        "Templates MUST use only allowed channel, direction, and actorType enum values or x-prefixed extensions.",
        "Processors SHOULD allow multiple correspondence configs to target the same kernel additively."
      ]
    },
    {
      "spec": "wos-spec/specs/governance/policy-parameters.md",
      "schema": "wos-spec/schemas/governance/wos-policy-parameters.schema.json",
      "llm": "wos-spec/specs/governance/policy-parameters.llm.md",
      "behaviorEssentials": [
        "Externalize date-indexed business rules as temporal parameters resolved against a date in the case state.",
        "Support value-over-time resolution (OpenFisca-style) for both scalar parameters and document bindings (URIs).",
        "Resolve parameters at evaluation time using the most recent effective date relative to the resolution date cursor.",
        "Inject resolved values into the execution context (under parameters.X) for use in FEL expressions and transitions."
      ],
      "conformanceEssentials": [
        "A conforming policy parameter document must include $wosPolicyParameters=1.0 and targetWorkflow.",
        "Resolution MUST be deterministic: only one value per parameter is effective at any given point in time.",
        "Processors should warn on parameter resolution gaps where the resolution date precedes the earliest effective date."
      ]
    },
    {
      "spec": "wos-spec/specs/governance/due-process-config.md",
      "schema": "wos-spec/schemas/governance/wos-due-process.schema.json",
      "llm": "wos-spec/specs/governance/due-process-config.llm.md",
      "behaviorEssentials": [
        "Detail operational due process implementation (notice templates, explanation rules, routing) separate from governance structure.",
        "Declare notice requirements for adverse decisions, including required sections and Formspec definition references.",
        "Define appeal routing rules and independence constraints to satisfy procedural fairness requirements.",
        "Describe continuation-of-service policies that maintain benefits or status during active appeal windows."
      ],
      "conformanceEssentials": [
        "A conforming due process document must include $wosDueProcess=1.0 and targetGovernance.",
        "Notice templates MUST include all sections declared required by the governing Workflow Governance document.",
        "Processors MUST enforce the declared independence constraints for appeal reviewers (pool-based or actor-based)."
      ]
    },
    {
      "spec": "wos-spec/specs/governance/assertion-library.md",
      "schema": "wos-spec/schemas/governance/wos-assertion-gate.schema.json",
      "llm": "wos-spec/specs/governance/assertion-library.llm.md",
      "behaviorEssentials": [
        "Declare reusable data validation logic gates that can be shared across multiple pipelines and governance documents.",
        "Categorize assertions by type (source-grounded, temporal, cross-document) to guide processor optimization and audit tagging.",
        "Provide FEL-based expressions for data constraints that evaluate to true for valid data and false for violations.",
        "Attach default rejection policies (retry, escalate, hold) to assertions to drive automated or human recovery."
      ],
      "conformanceEssentials": [
        "A conforming assertion library document must include $wosAssertionLibrary=1.0 and at least one assertion.",
        "Assertion IDs MUST be unique within the library and SHOULD follow persistent naming stability across versions.",
        "Processors should reject libraries with syntax-invalid FEL expressions in assertion definitions."
      ]
    },
    {
      "spec": "wos-spec/specs/ai/agent-config.md",
      "schema": "wos-spec/schemas/ai/wos-agent-config.schema.json",
      "llm": "wos-spec/specs/ai/agent-config.llm.md",
      "behaviorEssentials": [
        "Parameterize agent-specific model configurations (endpoint, model name, temperature, context limit) for WOS integration.",
        "Declare fallback strategies for model failure, including secondary models, parameter shifts, and human escalation.",
        "Fine-tune deontic constraints and autonomy levels per-action for a specific agent instance.",
        "Configure confidence scoring requirements that determine when an agent transition requires supervisory approval."
      ],
      "conformanceEssentials": [
        "A conforming agent config document must include $wosAgentConfig=1.0 and targetWorkflow.",
        "Agent references MUST match registration IDs in the corresponding AI Integration sidecar.",
        "Processors SHOULD treat provided model parameters as implementation-specific hints while enforcing WOS constraint logic."
      ]
    },
    {
      "spec": "wos-spec/specs/ai/drift-monitor.md",
      "schema": "wos-spec/schemas/ai/wos-drift-monitor.schema.json",
      "llm": "wos-spec/specs/ai/drift-monitor.llm.md",
      "behaviorEssentials": [
        "Monitor agent behavioral stability over time using statistical metrics (PSI, KS) and performance benchmarks (accuracy).",
        "Detect 'rubber-stamping' where human reviewers agree with agent output at rates or speeds that indicate a lack of engagement.",
        "Define automated remediation actions (alerts, demotion to assistive, circuit breaking) triggered by drift threshold violations.",
        "Manage model version transitions through shadow, canary, and production phases to ensure governance continuity."
      ],
      "conformanceEssentials": [
        "A conforming drift monitor document must include $wosDriftMonitor=1.0 and targetWorkflow.",
        "Statistical methods and evaluation windows MUST be applied as declared for each monitored agent.",
        "Processors MUST verify that shadow-mode deployments do not alter the authoritative case state or provenance."
      ]
    },
    {
      "spec": "wos-spec/specs/advanced/verification-report.md",
      "schema": "wos-spec/schemas/advanced/wos-verification-report.schema.json",
      "llm": "wos-spec/specs/advanced/verification-report.llm.md",
      "behaviorEssentials": [
        "Standardize the recording of results from formal verification (SMT proofs) of workflow governance properties.",
        "Capture result status (proven-safe, proven-unsafe, inconclusive) per-constraint with solver metadata and timestamps.",
        "Record counterexamples for unsafe constraints to assist in remediation of flawed workflow logic.",
        "Treat reports as immutable provenance artifacts that provide the reasoning foundation for high-stakes automation choices."
      ],
      "conformanceEssentials": [
        "A conforming verification report must include $wosVerificationReport=1.0, targetWorkflow, and results.",
        "Results MUST identify the exact version of the specification being verified to maintain the proof-to-document link.",
        "Solver metadata MUST include the tool version and configuration parameters used for the proof."
      ]
    },
    {
      "spec": "wos-spec/specs/advanced/equity-config.md",
      "schema": "wos-spec/schemas/advanced/wos-equity.schema.json",
      "llm": "wos-spec/specs/advanced/equity-config.llm.md",
      "behaviorEssentials": [
        "Configure disparity monitoring for protected categories across both human and AI decision points.",
        "Specify statistical methods (e.g., impact ratio, odds ratio) and reporting schedules for equity guardrails.",
        "Define remediation triggers that activate structured reviews when statistical disparity exceeds a defined bias cap.",
        "Enable category-level monitoring that does not require individual-level demographic data in the primary case record."
      ],
      "conformanceEssentials": [
        "A conforming equity document must include $wosEquityConfig=1.0 and targetWorkflow.",
        "Category definitions MUST NOT conflict with the privacy constraints of the integration environment.",
        "Processors MUST apply the declared disparity calculation method exactly as specified to ensure result comparability over time."
      ]
    },
    {
      "spec": "wos-spec/specs/sidecars/business-calendar.md",
      "schema": "wos-spec/schemas/sidecars/wos-business-calendar.schema.json",
      "llm": "wos-spec/specs/sidecars/business-calendar.llm.md",
      "behaviorEssentials": [
        "Declare working hours, weekend rules, and holiday schedules for time-sensitive SLA and deadline calculations.",
        "Support regional holiday libraries and ad-hoc exceptions (one-time closures) to ensure calendar accuracy.",
        "Define 'grace day' logic where deadlines that fall on non-working days are automatically shifted to the next business day.",
        "Integrate with Governance holds to exclude non-working time from SLA 'aging' calculations."
      ],
      "conformanceEssentials": [
        "A conforming business calendar document must include $wosBusinessCalendar=1.0 and weekend/holiday rules.",
        "Holiday definitions MUST include unambiguous effective dates (fixed, floating, or calculated).",
        "Processors MUST apply calendar logic consistently across all time-indexed logic triggers in the workflow."
      ]
    },
    {
      "spec": "wos-spec/specs/sidecars/notification-template.md",
      "schema": "wos-spec/schemas/sidecars/wos-notification-template.schema.json",
      "llm": "wos-spec/specs/sidecars/notification-template.llm.md",
      "behaviorEssentials": [
        "Define standardized notice templates for human and agent communication triggered by lifecycle events.",
        "Support multi-channel content (subject, body, SMS text) and attachments bound to specific notification types.",
        "Declare FEL-based variable requirements for template interpolation to ensure data availability at notice-generation time.",
        "Track notification delivery status and receipt in the provenance record for due process verification."
      ],
      "conformanceEssentials": [
        "A conforming notification template document must include $wosNotificationTemplate=1.0 and at least one template.",
        "Templates SHOULD declare an audience hint (human, agent, internal) to guide presentation and tone.",
        "Processors should warn if required FEL variables are missing from the case state when a notice is enqueued."
      ]
    },
    {
      "spec": "wos-spec/specs/registry/extension-registry.md",
      "schema": "wos-spec/schemas/registry/wos-extension-registry.schema.json",
      "llm": "wos-spec/specs/registry/extension-registry.llm.md",
      "behaviorEssentials": [
        "Catalog the kernel's six named extension seams (actorExtension, contractHook, provenanceLayer, lifecycleHook, custodyHook, vendor x- extensions) plus vendor-specific shape registrations.",
        "Track each registry entry through a draft → stable → deprecated → retired lifecycle with explicit replacedBy chains so consumers can follow migrations deterministically.",
        "Declare composition semantics (merge / replace / augment) per entry so multiple registrations at one seam combine predictably.",
        "Provide deterministic discovery and resolution: when multiple registries declare the same seam, the resolution order is published and stable."
      ],
      "conformanceEssentials": [
        "A conforming extension-registry document must include $wosExtensionRegistry=1.0, version, and at least one entry.",
        "Every entry MUST declare seam, kind, lifecycle, and description; entries that change vendor extensions MUST declare a vendorPrefix matching ^x-[a-z][a-z0-9-]*$.",
        "Processors MUST treat unknown registry entries as forward-compatible additions and MUST NOT reject documents that reference unregistered seams unless strict validation is requested."
      ]
    },
    {
      "spec": "wos-spec/specs/companions/runtime.md",
      "schema": "wos-spec/schemas/companions/wos-case-instance.schema.json",
      "llm": "wos-spec/specs/companions/runtime.llm.md",
      "behaviorEssentials": [
        "Execute WOS workflows through exactly four evaluation layers: Kernel (L0), Governance (L1), AI (L2), and Advanced (L3).",
        "Apply sidecar documents as a layered sieve: each layer refines and constrains the behavior allowed by the substrate.",
        "Enforce synchronous, atomic transitions where all layer constraints MUST hold before case state and provenance are committed.",
        "Maintain crash-safety and resume-capability by serializing the complete execution state as a Case Instance."
      ],
      "conformanceEssentials": [
        "A WOS-conformant processor MUST satisfy all Runtime G1-G6 durability and atomicity requirements.",
        "Processor evaluation MUST follow the normative layer order to prevent governance bypass.",
        "The processor MUST reject any transition where an L1-L3 constraint fails, even if the L0 transition is valid."
      ]
    },
    {
      "spec": "wos-spec/specs/companions/lifecycle-detail.md",
      "schema": "wos-spec/schemas/companions/wos-lifecycle-detail.schema.json",
      "llm": "wos-spec/specs/companions/lifecycle-detail.llm.md",
      "behaviorEssentials": [
        "Implement a SCXML-compatible statechart engine with support for atomic, compound, parallel, and final states.",
        "Apply deterministic transition selection based on document order and hierarchy depth (ancestor-first).",
        "Manage compensable scopes using backward-recovery (saga) patterns to undo effects of failed long-running activities.",
        "Provide durable timer semantics that ensure timeouts fire reliably even across processor restarts."
      ],
      "conformanceEssentials": [
        "Lifecycle implementations MUST adhere to the normative transition algorithm specified in Section 4.",
        "Parallel regions MUST support the declared cancellation policies: wait-all, cancel-siblings, and fail-fast.",
        "Compensation MUST execute compensating actions in reverse completion order within each compensable scope."
      ]
    },
    {
      "spec": "wos-spec/specs/profiles/integration.md",
      "schema": "wos-spec/schemas/profiles/wos-integration-profile.schema.json",
      "llm": "wos-spec/specs/profiles/integration.llm.md",
      "behaviorEssentials": [
        "Define compliance requirements for system-to-system interoperability in the WOS ecosystem.",
        "Declare supported event transport protocols (Webhook, CloudEvents, AMQP) and security profiles (mTLS, OAuth2).",
        "Establish data mapping requirements for projection between WOS case state and host-legacy systems.",
        "Identify policy engine adapters (OPA, Cedar) that provide authoritative external decision support."
      ],
      "conformanceEssentials": [
        "A conforming integration profile must include $wosIntegrationProfile=1.0 and targetWorkflow.",
        "Profiles MUST specify the required telemetry and observability hooks for WOS process monitoring.",
        "Security profiles MUST meet the minimum complexity requirements for the workflow impact level."
      ]
    },
    {
      "spec": "wos-spec/specs/profiles/semantic.md",
      "schema": "wos-spec/schemas/profiles/wos-semantic-profile.schema.json",
      "llm": "wos-spec/specs/profiles/semantic.llm.md",
      "behaviorEssentials": [
        "Standardize semantic alignments between WOS case state and global vocabularies (JSON-LD, Schema.org).",
        "Map WOS provenance records to PROV-O RDF shapes for cross-organizational audit exchange.",
        "Establish SHACL shapes for formal data quality verification of WOS documents and instances.",
        "Enable export to IEEE 1849 (XES) format for advanced process mining and bottleneck analysis."
      ],
      "conformanceEssentials": [
        "A conforming semantic profile must include $wosSemanticProfile=1.0 and targetWorkflow.",
        "JSON-LD context bindings MUST resolve to the declared ontology URIs.",
        "Semantic exports MUST preserve the WOS impactLevel and auditLayer metadata as required for provenance integrity."
      ]
    }
  ]
}
