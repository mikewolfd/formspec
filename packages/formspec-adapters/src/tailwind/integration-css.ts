/** @filedesc Tailwind integration CSS — minimal, since Tailwind utilities don't conflict with formspec layout. */

// Tailwind utilities are low-specificity and composable. They don't set max-width on inputs
// or add margins that conflict with formspec's grid/stack layout. Near-zero integration CSS needed.
export const integrationCSS = '';
