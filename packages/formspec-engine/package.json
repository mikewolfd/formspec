{
  "name": "formspec-engine",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test:unit": "node --experimental-specifier-resolution=node --test tests",
    "test": "npm run build && npm run test:unit"
  },
  "dependencies": {
    "@preact/signals-core": "^1.6.0",
    "chevrotain": "^11.1.1"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
