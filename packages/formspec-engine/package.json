{
  "name": "formspec-engine",
  "description": "Client-side form state engine with FEL expression language, reactive signals, and validation",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build:wasm": "wasm-pack build ../../crates/formspec-wasm --target bundler --out-dir ../../packages/formspec-engine/wasm-pkg",
    "build": "tsc",
    "test:unit": "node --experimental-specifier-resolution=node --import ./tests/setup.mjs --test \"tests/**/*.test.mjs\"",
    "test": "npm run build && npm run test:unit"
  },
  "dependencies": {
    "@preact/signals-core": "^1.6.0",
    "ajv": "^8.18.0",
    "chevrotain": "^11.1.1",
    "formspec-types": "*"
  },
  "devDependencies": {
    "fast-check": "^4.6.0",
    "typescript": "^5.0.0"
  }
}
