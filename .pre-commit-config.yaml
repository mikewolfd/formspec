# Pre-commit hooks disabled during Rust layout planner development.
# The build-and-test hook's stash/unstash cycle conflicts with
# incremental WASM builds when unstaged Rust changes are present.
# Re-enable after branch merges to main.
repos: []
