# cargo-deny configuration (ADR 0008 ISC-05 hygiene contract).
# Install: cargo install cargo-deny
# Run: cargo deny check bans

[graph]
targets = [
    { triple = "x86_64-unknown-linux-gnu" },
    { triple = "aarch64-apple-darwin" },
]

[bans]
multiple-versions = "warn"
wildcards = "warn"

# Ecosystem libraries are forbidden outside their designated adapter crate.
# If a core crate (trellis-core, trellis-verify, trellis-types) transitively
# pulls in any of these, cargo-deny fails loud.
deny = [
    { name = "c2pa", wrappers = ["trellis-interop-c2pa"] },
    { name = "scitt", wrappers = ["trellis-interop-scitt"] },
    { name = "ssi-*", wrappers = ["trellis-interop-vc", "trellis-interop-did"] },
    { name = "did-method-key", wrappers = ["trellis-interop-did"] },
    { name = "json-ld", wrappers = ["trellis-interop-vc"] },
    { name = "rdf-canonicalization", wrappers = ["trellis-interop-vc"] },
]
